# KOAS R&D UI·UX 개선 및 코드 검토

> 후속 업데이트: 아래 문제 목록은 최초 검토 기록입니다. 동시 저장, 관리자 권한, HTML 입력 처리, 실시간 반영은 이후 수정했습니다. 현재 구현·테스트 결과와 운영 적용 절차는 [SECURITY-적용안내.md](SECURITY-적용안내.md)를 확인하세요.

2026-09-09 / 제공된 로컬 index.html 기준. GitHub 원격 저장소와 Firebase 운영 설정은 제공되지 않아 확인하지 않았습니다.

## 적용한 개선

- 폰트를 Noto Sans KR로 통일하고, 사용하지 않는 Pretendard 폰트 리소스를 제거했습니다. 독립 실행용 HTML 구조를 유지하면서 약 10.04MB → 6.06MB로 줄였습니다.
- 업무 화면의 본문·제목·보조 문구 크기, 카드 테두리, 여백, 파란색 강조를 정리했습니다. 사이드바 로고를 복원하고 현재 메뉴를 표시합니다.
- 좁은 화면에서 메뉴 이름이 사라지는 방식을 이름이 보이는 접이식 메뉴로 변경했습니다. 닫기 버튼, 배경 클릭, Escape, 메뉴 내부 키보드 이동을 지원합니다.
- 대시보드의 고정된 14건과 잘못된 이동 경로를 제거했습니다. 설계변경 등록 항목 수와 완료 프로젝트 수를 실제 로드된 데이터로 계산합니다. 완료 기준이 없는 설계변경 행을 임의로 '진행 중'이라고 판단하지 않습니다.
- 표 스크롤 영역, 고정 헤더, 행 간격, 편집 입력 크기, 가로 스크롤 설명을 추가했습니다.
- 로그인 제목, 입력 레이블 연결, 자동완성, 비밀번호 표시/숨김, 오류 알림 접근성을 개선했습니다.
- 동작이 없는 알림 아이콘과 목적지가 없는 하단 링크를 제거했습니다. 정책 문서가 준비되면 실제 주소로 복원해야 합니다.
- 저장 중/완료/실패 안내, 오프라인 안내, 실패 토스트를 추가했습니다. 관리대장 저장 실패 시 편집 상태를 유지합니다. 편집 중이거나 저장에 실패한 상태에서 브라우저를 닫을 때 경고합니다.
- 키보드로 메뉴·카드를 실행하고, 검색 단축키 Ctrl/⌘+K와 본문 바로가기를 사용할 수 있습니다.
- 대시보드 재진입 시 시계 타이머가 누적되는 문제와, 의도적으로 비운 관리대장에 기본 데이터가 다시 나타나는 문제를 수정했습니다.
- 초기 데이터 로드가 실패한 상태에서는 전체 데이터를 저장하지 못하도록 막았습니다.

## 남아 있는 문제와 개선 우선순위

### 1. 권한 검증 — 높음

관리자 이메일 비교와 일부 버튼 숨김을 브라우저 코드에서 처리합니다. 이것만으로 실제 데이터 접근 권한을 보장할 수 없습니다. Firebase 보안 규칙이 이 폴더에 없으므로 실제 취약 여부는 미확인입니다. 운영 Firestore Rules에서 승인된 회원 여부, 관리자 역할 변경 권한, 문서별 읽기·쓰기 권한을 점검해야 합니다. 필요하면 Custom Claims와 서버 측 관리자 작업으로 분리합니다.

### 2. 동시 편집과 전체 덮어쓰기 — 높음

saveAppData가 프로젝트, 게시판, 관리대장을 appData/main 문서 하나에 저장합니다. 두 사용자가 각각 수정하면 뒤늦은 저장이 상대방의 변경을 덮어쓸 수 있습니다. 프로젝트·게시글·관리대장 행을 개별 문서로 분리하고, 변경된 필드만 저장하며 수정 버전/트랜잭션으로 충돌을 감지하는 개선이 필요합니다. 데이터 이관이 필요해 이번 화면 변경에서 저장 스키마를 바꾸지는 않았습니다.

### 3. 사용자 입력 HTML 삽입 — 높음

프로젝트 이름, 그룹 이름, 회원 이름 등 여러 값을 innerHTML과 인라인 이벤트 문자열에 넣습니다. 일반 텍스트는 textContent로 렌더링하고, 이벤트는 addEventListener로 분리해야 합니다. 서식을 허용하는 게시글·메모는 허용 태그 기반 정화가 필요합니다. 실제 공격을 실행하거나 운영 데이터를 수정하지 않았으며, 현재 입력 경로 전체가 안전하다고 검증한 상태는 아닙니다.

### 4. 실시간 협업과 저장 상태 — 중간~높음

원본은 onSnapshot을 가져오지만 호출하지 않으며 getDoc으로 데이터를 읽습니다. 다른 사용자의 변경이 자동 반영되는 구조가 아닙니다. 이번에는 오해를 주는 '실시간' 문구를 제거했습니다. 문서 분리 후 구독, 충돌 알림, 마지막 수정자·시각을 제공하는 편이 좋습니다. 현재 저장 표시도 다른 사용자의 상태를 뜻하지 않습니다.

관리대장 외의 기존 작업은 저장 성공 전에 화면 전환이나 성공 문구를 보여 줄 수 있습니다. 공통 저장 오류 알림은 추가했지만 모든 작업을 트랜잭션 형태로 전환하지는 않았습니다. 프로젝트 상세·게시판도 저장 완료를 기다린 뒤 편집을 종료하고 실패 시 재시도하도록 통일하는 후속 작업이 필요합니다. 화면 내부 이동 전 편집 내용을 보호하는 확인 흐름도 보완 대상입니다.

### 5. 회원 관리 — 중간

회원 삭제·가입 거절은 Firestore 회원 기록만 지우고 Authentication 계정은 남깁니다. 관리자 서버 함수에서 인증 계정과 회원 데이터를 함께 처리해야 합니다. 가입 승인 대기 화면도 승인 상태를 자동 구독하지 않습니다. 회원가입 중복 제출 방지와 승인 완료 자동 진입을 함께 개선하면 좋습니다.

### 6. 이미지와 배포 데이터 — 중간

Cloudinary 업로드 설정이 브라우저에 있으며, 실제 프리셋 제한은 미확인입니다. 업로드 크기·허용 형식·폴더·삭제 권한을 서비스 설정에서 검토해야 합니다. 또한 HTML의 DEFAULT 데이터에 업무 항목이 내장되어 있습니다. 공개 GitHub 저장소라면 내장 업무 내용과 과거 커밋의 공개 범위를 확인해야 합니다. Firebase의 클라이언트 설정이 보인다는 사실만으로 비밀키 유출이라고 판단하지는 않았습니다.

### 7. 운영성과 화면 품질 — 중간

단일 HTML은 올리기 쉽지만 원본 애플리케이션 코드가 큰 문자열로 묶여 있어 변경 검토와 유지보수가 어렵습니다. 장기적으로 인증·프로젝트·관리대장·게시판 모듈, 로컬 폰트·이미지 파일을 분리하면 캐시와 코드 검토에 유리합니다. 이번에는 ui/에 새 스타일과 동작을 분리하고 재생성 스크립트를 추가했습니다.

날짜에 '0004-59-24' 같은 비정상 기본값이 있고 연도가 제목에 고정되어 있습니다. 날짜 검증, 명확한 완료 상태, 담당자/상태 필터, 변경 이력, 저장 취소·복구를 단계적으로 도입하는 편이 좋습니다. 기존 다크모드에는 인라인 흰 배경과 고정 색상이 많아 모든 편집 팝업까지 일관되게 보이는지 추가 화면 검증이 필요합니다.

## 검증과 사용 방법

- 원본은 backups/index.original.html에 보관했습니다. backups/와 inspection/은 Git 제외 대상입니다.
- 배포 파일은 index.html 하나입니다. GitHub의 기존 파일을 대체할 수 있지만 이번 작업에서 push/배포하지 않았습니다.
- 스타일과 UI 동작: ui/improvements.css, ui/improvements.js.
- 재생성: `python tools/build.py`. 로컬 원본 백업이 필요합니다. 별도 개발 환경에 가져가려면 원본을 안전한 경로로 함께 전달해야 합니다.
- 검증: `node tools/verify.mjs`. 저장 실패 시 데이터 보호, 초기 로드 전 저장 차단, 편집 유지, 타이머 중복 방지 등을 독립된 모의 환경에서 확인했습니다.
- 번들 JSON, 내장 파일의 Base64/gzip 복원, 정적 ID 중복, JS 문법도 검사했습니다.
- 로그인·실제 저장·업로드·관리자 권한은 운영 계정으로 테스트하지 않았습니다. 브라우저 렌더링 및 모바일 실기기 검증도 아직 수행하지 않았으므로 배포 전 확인해야 합니다.
