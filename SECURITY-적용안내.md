# 공동 편집·보안 개선 적용 안내

## 현재 상태

로컬 코드와 Firestore 보안 규칙을 수정하고 demo-koas-security 로컬 에뮬레이터에서 8개 테스트를 통과했습니다. 운영 Firebase와 GitHub에는 배포하지 않았습니다. Firebase CLI에서 연결된 관리자 계정이 없는 것을 확인했습니다.

## 바뀐 동작

1. 저장 전에 처음 읽은 내용, 내 변경, 서버의 최신 내용을 비교합니다. 서로 다른 프로젝트 필드 변경은 합치고, 같은 필드나 같은 배열을 서로 다르게 편집하면 충돌로 중단합니다. 실패한 편집 내용과 다운로드 기능을 제공합니다.
2. 관리자 이메일 비교와 클라이언트의 관리자 프로필 자동 생성/승격을 제거했습니다. Firebase ID 토큰의 `admin: true` Custom Claim을 사용합니다. 일반 회원의 승인 상태는 Firestore Rules에서 검사합니다.
3. 44개 동적 마크업 템플릿의 보간 값을 문맥에 맞게 이스케이프하고, 49개 HTML 대입 지점에 안전한 렌더링을 적용했습니다. 회원·프로젝트·게시글은 문자열로 표시하고, 메모만 DOMPurify로 굵게/색상 등 제한된 서식을 허용합니다. 이벤트 문자열 안의 값은 유니코드 이스케이프로 보호하며 이미지 URL도 허용 프로토콜로 제한합니다.
4. Firestore `onSnapshot`으로 다른 사용자의 저장을 구독합니다. 편집하지 않는 화면은 갱신하고, 편집 중이면 수신 내용을 보류해 안내합니다. 회원 승인과 승인 철회도 구독합니다. 로그아웃과 권한 해제 시 구독을 정리합니다.

## 데이터 호환과 제한

- 데이터 이관 없이 기존 `appData/main`을 유지합니다. 새 저장에는 `revision`, `protocolVersion: 2`, `updatedBy`가 추가됩니다.
- 보안 규칙은 이전 버전의 전체 덮어쓰기 요청을 거절합니다. 모든 사용자가 새 화면으로 새로고침해야 합니다.
- 관리대장 행과 게시글 배열은 충돌 안전성을 위해 배열 단위로 비교합니다. 같은 관리대장의 서로 다른 행을 동시에 고쳐도 확인이 필요할 수 있습니다. 자동으로 합쳐 내용을 잃는 것보다 보수적으로 처리합니다.
- 한 문서의 용량 및 쓰기 집중 문제는 그대로입니다. 사용량이 커지면 행/게시글별 문서 분리가 필요합니다.
- Firebase 권한 규칙 배포와 관리자 Custom Claim 설정 전에는 보안 개선이 운영 서비스에 적용되었다고 볼 수 없습니다. 현재 관리자 이메일만으로 접근하던 계정은 Claim을 먼저 설정해야 합니다.

## 운영 적용 순서

1. Firebase Console에서 현재 Firestore Rules와 `appData/main`을 백업합니다. Firebase Authentication에서 실제 관리자 UID를 확인합니다.
2. 신뢰할 수 있는 관리자 환경에서 Application Default Credentials를 설정한 뒤 `node tools/set-admin.mjs koas-3fa14 <확인한-관리자-UID>`를 실행합니다. 이 스크립트는 기존 Custom Claims를 유지하며 관리자 권한만 추가합니다. 서비스 계정 키나 토큰을 이 저장소에 넣거나 채팅에 붙이지 마세요.
3. Firebase CLI의 `firebase login`으로 프로젝트 관리자가 로그인합니다. 확인된 프로젝트에 `firebase deploy --only firestore:rules --project koas-3fa14`로 이 폴더의 규칙을 배포합니다. 기존 버전의 저장은 이때부터 차단됩니다.
4. GitHub의 기존 배포 파일을 새 `index.html`로 교체합니다. 관리자와 회원은 새로고침하고, 관리자는 로그아웃 후 다시 로그인해 새 권한을 받습니다.
5. 관리자/승인 회원/미승인 회원 계정으로 접근을 확인하고, 서로 다른 두 브라우저에서 동시 편집과 알림을 확인합니다.

운영 규칙과 계정 설정을 확인하지 않은 상태에서 위 배포 명령을 자동 실행하지 않았습니다. 관리자 UID도 임의로 정하지 않았습니다.

## 검증

- `npm run build`: 원본 백업과 UI·동기화·보안 모듈에서 단일 HTML 재생성.
- `npm test`: 입력 공격, 병합 충돌, 네트워크 오류, 실시간 편집 보호 등을 검사. 에뮬레이터 연결이 없으면 서버 규칙 테스트 하나는 건너뜁니다.
- `npm run test:rules`: Java 21과 Firestore 에뮬레이터에서 서버 규칙을 포함한 전체 검사. 이번 작업에서는 8개 모두 통과했습니다.
- 서버 규칙 테스트: 익명 접근, 미승인 접근, 관리자 이메일 사칭, 본인 관리자 승격, 전체 회원 조회, 오래된 저장 요청 차단; 관리자 승인·승인 철회; 두 클라이언트의 실제 동시 트랜잭션 및 동일 필드 충돌.
- DOM 테스트: 이름/게시글의 악성 태그, 속성 삽입, 이벤트 문자열 탈출, 메모의 SVG/스크립트·위험 스타일 제거, 기존 카드 이동 유지.

참고: [Firebase 트랜잭션](https://firebase.google.com/docs/firestore/manage-data/transactions), [Firebase Custom Claims](https://firebase.google.com/docs/auth/admin/custom-claims), [DOMPurify 공식 문서](https://github.com/cure53/DOMPurify).
